services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - NEXT_DISABLE_TURBOPACK=1
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
